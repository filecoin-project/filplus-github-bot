name: Build

on:
  push:
    branches:
      - ci-cd-process

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm install
      - run: npm run build